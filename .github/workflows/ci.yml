name: CI

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr55-production-release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  no-further-release:
    name: Temporary release gate disabled
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
