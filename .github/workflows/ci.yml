name: CI

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pr55-final-production-promotion-${{ github.ref }}
  cancel-in-progress: false

jobs:
  no-further-release:
    name: Temporary promotion gate disabled
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo disabled
