name: Final email identity idempotency

on:
  push:
    branches:
      - agent/ai-response-hardening-20260808
  pull_request:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: final-email-identity-idempotency
  cancel-in-progress: true

jobs:
  finalize:
    if: ${{ !contains(github.event.head_commit.message || '', 'Finalize email identity idempotency') }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/ai-response-hardening-20260808
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply final email identity/idempotency fixes
        run: |
          python scripts/finalize-email-idempotency-v6.py
          python scripts/fix-v6-lifecycle-client-ips.py
          python scripts/fix-v6-manager-reset-semantics.py
      - name: Remove temporary finalizer
        run: |
          git rm -f scripts/finalize-email-idempotency-v6.py
          git rm -f scripts/fix-v6-lifecycle-client-ips.py
          git rm -f scripts/fix-v6-manager-reset-semantics.py
          git rm -f .github/workflows/finalize-email-idempotency-v6.yml
      - name: Stage sources and regenerate manifest
        run: |
          git add -A
          npm run manifest:source
          git add SOURCE_SHA256SUMS.txt SOURCE_SHA256SUMS.override.txt
      - name: Create local validation commit
        run: |
          git config user.name "CPCV Reliability Bot"
          git config user.email "actions@users.noreply.github.com"
          git commit -m "Finalize email identity idempotency"
      - name: Focused regressions
        run: |
          node --no-warnings scripts/test-account-lifecycle-v2.mjs
          node --no-warnings scripts/test-email-auth-v2.mjs
          node --no-warnings scripts/test-auth-api-v2.mjs
          node scripts/test-system-hardening.mjs
          npm run check
          npm run check:project
          npm run verify:source-manifest
          git diff --check
      - name: Full regression
        run: npm run check:stage08
      - name: Deployment and dependency checks
        run: |
          npm run deploy:dry-run
          npm audit
          npm audit --omit=dev
      - name: Push validated result
        run: git push origin HEAD:agent/ai-response-hardening-20260808
