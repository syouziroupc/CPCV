name: Temporary AI quality test checksum
on:
  pull_request:
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sha256sum scripts/test-ai-quality-ui-refinement.mjs
