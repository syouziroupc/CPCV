name: Temporary app CSS checksum
on:
  push:
    branches:
      - agent/fix-ai-runtime-throughput-20260807
jobs:
  hash:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sha256sum public/assets/app.css
