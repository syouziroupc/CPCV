name: Deploy TalkSys v15 once

on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/talksys-v15-deploy-once.yml'

permissions:
  contents: read

jobs:
  deploy-talksys:
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - name: Checkout TalkSys v15
        uses: actions/checkout@v6
        with:
          repository: syouziroupc/talksys
          ref: 66f4710062ad9c5b3a37be7ca6dcca68de4985f0
          path: talksys

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        working-directory: talksys
        run: npm install --no-audit --no-fund

      - name: Validate TalkSys v15
        working-directory: talksys
        run: |
          npm test
          npx wrangler deploy --dry-run

      - name: Require Cloudflare production credentials
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"

      - name: Deploy TalkSys v15 to Cloudflare
        working-directory: talksys
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler deploy

      - name: Verify TalkSys production revision
        env:
          PRODUCTION_URL: https://talksys.syouziroupc.workers.dev
        run: |
          for i in 1 2 3 4 5; do
            body="$(curl -fsS --max-time 20 "$PRODUCTION_URL/voice-health" || true)"
            revision="$(BODY="$body" node -e "let d={};try{d=JSON.parse(process.env.BODY||'{}')}catch{};process.stdout.write(String(d.voiceRevision||''))")"
            echo "Attempt $i: ${revision:-missing}"
            if [ "$revision" = "cloudflare-live-v15.0" ]; then
              exit 0
            fi
            sleep 3
          done
          exit 1
