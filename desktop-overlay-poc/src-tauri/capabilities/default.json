{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-controller",
  "description": "Permissions for the bundled CPCV overlay controller only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
